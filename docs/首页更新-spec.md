# 首页更新 Spec

> 版本: 1.0.0  
> 更新日期: 2026-03-19  
> 关联文档: 比升技术官网对外宣传内容框架.md

---

## 一、更新内容

### 1.1 Hero区域核心标签

在"AI时代企业数智服务商"标题下方增加三个核心标签：

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│              AI时代企业数智服务商                         │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ AI原生中台底座 │  │   轻咨询     │  │   AI赋能     │  │
│  │   稳定可靠    │  │  共创价值    │  │ 深融业务场景  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**标签设计：**
| 标签 | 主标题 | 副标题 | 图标建议 |
|------|--------|--------|----------|
| 1 | AI原生中台底座 | 稳定可靠 | 服务器/架构图标 |
| 2 | 轻咨询 | 共创价值 | 握手/合作图标 |
| 3 | AI赋能 | 深融业务场景 | AI/智能图标 |

**样式要求：**
- 标签采用卡片式设计，带有轻微阴影
- 主标题使用较大字号，副标题较小
- 可选：添加图标增强视觉效果
- 响应式：移动端纵向排列

---

### 1.2 Hero背景动效

**背景效果设计：**

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│    ✨ 流光粒子效果（科技感）                              │
│                                                         │
│         ┌─────────────┐                                 │
│         │   魔方图形   │  ← 发光魔方，缓慢旋转            │
│         │  (3D效果)   │                                 │
│         └─────────────┘                                 │
│                                                         │
│    ✨ 流光粒子效果（科技感）                              │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**动效实现方案：**

1. **流光效果**
   - 使用CSS动画实现光线流动
   - 多层渐变叠加
   - 颜色：蓝色/紫色渐变（科技感）

2. **发光魔方**
   - 方案A：CSS 3D Transform实现旋转魔方
   - 方案B：SVG动画实现发光效果
   - 效果：缓慢旋转，边缘发光

**CSS动效代码示例：**

```css
/* 流光背景 */
.hero-bg {
  background: linear-gradient(135deg, #0a0a1a 0%, #1a1a3a 100%);
  position: relative;
  overflow: hidden;
}

.hero-bg::before {
  content: '';
  position: absolute;
  width: 200%;
  height: 200%;
  background: radial-gradient(ellipse at center, 
    rgba(59, 130, 246, 0.1) 0%, 
    transparent 70%);
  animation: pulse 8s ease-in-out infinite;
}

/* 发光魔方 */
.magic-cube {
  animation: rotateCube 20s linear infinite;
  filter: drop-shadow(0 0 30px rgba(59, 130, 246, 0.5));
}

@keyframes rotateCube {
  from { transform: rotateY(0deg) rotateX(15deg); }
  to { transform: rotateY(360deg) rotateX(15deg); }
}

@keyframes pulse {
  0%, 100% { opacity: 0.3; transform: scale(1); }
  50% { opacity: 0.6; transform: scale(1.1); }
}
```

---

## 二、技术实现

### 2.1 Hero组件更新

**新增元素：**
1. 核心标签容器（CoreTags组件）
2. 背景动效层（BackgroundEffect组件）

**组件结构：**
```
HeroSection
├── BackgroundEffect (背景动效)
│   ├── FlowingLights (流光效果)
│   └── MagicCube (发光魔方)
├── ContentLayer (内容层)
│   ├── Title (标题)
│   ├── CoreTags (核心标签)
│   └── CTAButtons (行动按钮)
```

### 2.2 JSON数据更新

```json
{
  "hero": {
    "title": "AI时代企业数智服务商",
    "subtitle": "全国领先的全渠道数智运营服务提供商",
    "coreTags": [
      {
        "title": "AI原生中台底座",
        "subtitle": "稳定可靠",
        "icon": "platform"
      },
      {
        "title": "轻咨询",
        "subtitle": "共创价值",
        "icon": "consulting"
      },
      {
        "title": "AI赋能",
        "subtitle": "深融业务场景",
        "icon": "ai"
      }
    ],
    "cta": {
      "primary": "预约演示",
      "secondary": "了解更多"
    }
  }
}
```

---

## 三、视觉效果

### 3.1 配色方案

| 元素 | 颜色 |
|------|------|
| 背景主色 | #0a0a1a (深蓝黑) |
| 流光色 | #3b82f6 (蓝色) → #8b5cf6 (紫色) |
| 魔方发光 | rgba(59, 130, 246, 0.5) |
| 标签背景 | rgba(255, 255, 255, 0.1) |
| 标签文字 | #ffffff |

### 3.2 动效参数

| 动效 | 时长 | 缓动函数 |
|------|------|----------|
| 流光脉冲 | 8s | ease-in-out |
| 魔方旋转 | 20s | linear |
| 标签悬停 | 0.3s | ease |

---

## 四、验收标准

- [ ] 三个核心标签正确显示
- [ ] 背景流光效果流畅
- [ ] 发光魔方动效正常
- [ ] 不影响页面性能（FPS > 30）
- [ ] 响应式布局正常
- [ ] 移动端动效可降级处理
